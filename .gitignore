.research/
.noob/

# local agent-CLI bake-off, not published
/competition/

# build caches (docker bind mounts, user-owned)
/target
/gui/target
/.cargo-home
/.rustup-home
/workspace/

# real config lives here when the repo checkout is used as the /config mount;
# only the example and the contract are committed
/config/*
!/config/.env.example
!/config/skills/
!/config/skills/web-search/
!/config/skills/web-search/SKILL.md

# internal working docs, not published
/docs/RENDER-NOTES.md
/docs/STATE.md
/docs/EMISSION-MAP.md
