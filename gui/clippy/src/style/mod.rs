//! Transcript presentation as one box: the palette derived from config
//! (skin), syntax colors for the file view (syntax), and markdown to styled
//! runs (markdown). Three files, one chain: config in, styled runs out.

pub mod markdown;
pub mod skin;
pub mod syntax;
