[package]
name = "noob-testkit"
description = "Dev-only test rig: mock OpenAI and MCP servers with automatic wire assertions, a pty driver, a terminal screen emulator."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde_json.workspace = true

[target.'cfg(unix)'.dependencies]
libc.workspace = true
