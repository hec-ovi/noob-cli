Build a retro Snake arcade game as a single self-container file named game.html (HTML + CSS + JS, one <canvas>, NO external assets, CDNs, or libraries). Crisp retro NES pixel-art aesthetic: bold readable palette, chunky pixel sprites, everything drawn on canvas. A snake made of chunky square segments moves on a grid and is steered with the arrow keys; it grows by one segment each time it eats a food pellet, and a new pellet then appears at a random empty cell; the game ends when the snake runs into a wall or into its own body. Show a minimal HUD with SCORE. It must run by just opening game.html. Also add a self-playing demo mode: when the URL has ?autoplay=1, a built-in bot plays the game automatically (no human input) and loops forever so it always looks alive. Create the file now.
