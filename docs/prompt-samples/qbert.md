Build a retro Q*bert arcade game as a single self-container file named game.html (HTML + CSS + JS, one <canvas>, NO external assets, CDNs, or libraries). Crisp retro NES pixel-art aesthetic: bold readable palette, chunky pixel sprites, everything drawn on canvas. An isometric pyramid of 28 cubes (7 rows), Q*bert starts on the top cube and hops diagonally between adjacent cubes with the arrow keys; every cube he lands on flips its top to the target color, and when all cubes are flipped the level is complete; hopping off the pyramid edge costs a life. Show a minimal HUD with SCORE. It must run by just opening game.html. Also add a self-playing demo mode: when the URL has ?autoplay=1, a built-in bot plays the game automatically (no human input) and loops forever so it always looks alive. Create the file now.
