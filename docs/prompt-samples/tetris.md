Build a retro Tetris arcade game as a single self-container file named game.html (HTML + CSS + JS, one <canvas>, NO external assets, CDNs, or libraries). Crisp retro NES pixel-art aesthetic: bold readable palette, chunky pixel sprites, everything drawn on canvas. The seven tetromino pieces fall one at a time onto a 10 by 20 grid; the arrow keys move the piece left and right, rotate it, and soft-drop it; completing a full horizontal row clears it and scores points; the game ends when a newly spawned piece has no room to appear. Show a minimal HUD with SCORE. It must run by just opening game.html. Also add a self-playing demo mode: when the URL has ?autoplay=1, a built-in bot plays the game automatically (no human input) and loops forever so it always looks alive. Create the file now.
